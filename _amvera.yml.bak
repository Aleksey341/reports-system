meta:
  environment: node18

build:
  commands:
    - npm ci

run:
  command: npm start

port: 80

healthcheck:
  path: /health
  interval: 30
  timeout: 10

scaling:
  min_replicas: 1
  max_replicas: 3
